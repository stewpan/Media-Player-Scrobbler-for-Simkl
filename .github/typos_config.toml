# Typos configuration file

[default]
extend-ignore-re = [
  # Add regexes to ignore specific patterns
]

[default.extend-words]
# Add exceptions to the dictionary
datas = "datas"
gir = "gir"
# Ordinal suffix used in season-matching regex/logic (e.g. "2nd", "3rd")
nd = "nd"
